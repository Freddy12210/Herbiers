{% extends 'base.html.twig' %}

{% block title %}Nouveau relevé{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Créer un nouveau relevé</h1>
        {{ include('releves/_form.html.twig') }}
        <a href="{{ path('app_releves_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
